import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { RenewalPage } from './renewal';

@NgModule({
  declarations: [
    RenewalPage,
  ],
  imports: [
    IonicPageModule.forChild(RenewalPage),
  ],
})
export class RenewalPageModule {}
